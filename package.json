{
  "name": "powitania-app",
  "version": "1.0.0",
  "description": "powitania.pl - Agencja Kreatywna",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build:css": "npx tailwindcss -i src/input.css -o public/css/tailwind.css --minify",
    "pull-prod": "node scripts/pull-from-prod.js",
    "check-sync": "node scripts/check-prod-sync.js",
    "prepare": "git config core.hooksPath .githooks 2>/dev/null || true"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ejs": "^3.1.10",
    "express": "^4.21.0",
    "multer": "^2.0.2",
    "sharp": "^0.34.5"
  }
}
